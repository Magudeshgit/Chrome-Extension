import React from 'react'
import { Create_mgaccount } from '@/components/create_mgaccount'

const Create_mgaccount = (props) => {
  return (
   <Create_mgaccount email={props.email}/> 
  )
}

export default C